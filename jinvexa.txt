

# ==================== OLLAMA LLM CONFIG ====================
MODEL=gemma4:31b-cloud
OLLAMA_MODEL=gemma4:31b-cloud

# ==================== STORAGE CONFIGURATION ====================
# Switch between 'json' (local file system) and 'mongodb' (database)
STORAGE_TYPE=mongodb
STORAGE_DIR=memory_storage

# For MongoDB Atlas (Cloud Cluster):
# Replace <username> and <password> with your database user credentials
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.jinvexa.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB_NAME=jinvexa_production
